[package]
name = "vertices-crypto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Hardware-agnostic signing interfaces for Vertices Network"

[features]
default = []
software = ["dep:ed25519-dalek"]

[dependencies]
vertices-core = { path = "../vertices-core" }
ed25519-dalek = { workspace = true, optional = true }
